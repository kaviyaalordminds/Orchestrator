# Orchestrator Backend — Environment Configuration
# Copy this file to .env and fill in the real values.
# NEVER commit .env to version control.

# ─── Obsidian Local REST API ───────────────────────────────────────────────
# The plugin runs at https://127.0.0.1:27124 by default.
OBSIDIAN_API_URL=https://127.0.0.1:27124

# Find your API key in Obsidian → Settings → Local REST API with MCP
OBSIDIAN_API_KEY=your-obsidian-api-key-here

# Keep False for the local self-signed certificate
OBSIDIAN_VERIFY_SSL=false

# ─── Qdrant Vector Database ────────────────────────────────────────────────
# Local on-disk path for vector persistence (relative to backend/)
# IMPORTANT: must be an ABSOLUTE path (Qdrant treats relative paths as remote URLs)
QDRANT_LOCATION=/absolute/path/to/your/orchestrator/backend/data/qdrant
QDRANT_COLLECTION=obsidian_vault
QDRANT_VECTOR_SIZE=768

# ─── LLM — Ollama (local) ─────────────────────────────────────────────────
OLLAMA_URL=http://127.0.0.1:11434
EMBEDDING_MODEL=nomic-embed-text
LLM_MODEL=llama3

# ─── RAG Tuning ────────────────────────────────────────────────────────────
RAG_TOP_K=5
RAG_CHUNK_SIZE=500
RAG_CHUNK_OVERLAP=100

# ─── Admin API Security ────────────────────────────────────────────────────
# Protect /api/v1/rag/index and /api/v1/rag/reindex
# Generate a strong random string, e.g.:  openssl rand -hex 32
ADMIN_API_KEY=your-admin-api-key-here

# ─── Audio Studio ──────────────────────────────────────────────────────────
# Local macOS TTS provider for Voice Over generation.
AUDIO_PROVIDER=macos_say
AUDIO_OUTPUT_DIR=/absolute/path/to/your/orchestrator/backend/data/audio
AUDIO_DEFAULT_VOICE=Samantha
AUDIO_DEFAULT_SPEED=175
